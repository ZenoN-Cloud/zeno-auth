# ───────────────
# Base
# ───────────────
ENV=dev
APP_NAME=zeno-auth
PORT=8080

# ───────────────
# CORS
# ───────────────
# Comma-separated list of allowed origins
# For production: https://app.zenon-cloud.com,https://console.zenon-cloud.com
# For development: http://localhost:5173,http://localhost:3000
CORS_ALLOWED_ORIGINS=http://localhost:5173

# ───────────────
# Logging
# ───────────────
LOG_LEVEL=debug        # debug | info | warn | error
LOG_FORMAT=json        # json | console
LOG_FILE=logs/app.log  # локально; в Cloud Run игнорируется

# ───────────────
# Database
# ───────────────
# В Cloud Run DATABASE_URL приходит из Secret Manager:
#   zeno-auth-db-dsn
#
# Для локальной разработки (через Cloud SQL Proxy или локальный Postgres):
DATABASE_URL=postgres://zeno_auth:CHANGE_ME@localhost:5432/zeno_auth?sslmode=disable

# ───────────────
# JWT Keys
# ───────────────
# ⚠️ NEVER commit real keys to repository!
#
# For local development:
#   1. Copy this file to .env.local
#   2. Generate keys: make generate-keys
#   3. Paste keys into .env.local
#   4. Add .env.local to .gitignore (already done)
#
# For production:
#   - Store in GCP Secret Manager / AWS Secrets Manager / Vault
#   - Load via environment variables at runtime
#   - NEVER hardcode in config files or commit to repo

JWT_PRIVATE_KEY="---YOUR-PRIVATE-KEY-HERE---"
JWT_PUBLIC_KEY="---YOUR-PUBLIC-KEY-HERE---"

# ───────────────
# Token TTL (секунды)
# ───────────────
ACCESS_TOKEN_TTL=1800        # 30 минут
REFRESH_TOKEN_TTL=1209600    # 14 дней

# ───────────────
# Misc
# ───────────────
TIMEZONE=UTC
