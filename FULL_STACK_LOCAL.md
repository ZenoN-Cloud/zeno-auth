# 🚀 Полная локальная обкатка Zeno CY (Full Stack)

Полноценная локальная разработка **Zeno Auth** (Backend) + **Zeno Console** (Frontend).

## 📦 Что запускается?

1. **PostgreSQL** - База данных (порт 5432)
2. **Zeno Auth API** - Backend сервис (порт 8080)
3. **Zeno Console** - React Frontend (порт 5173)
4. **pgAdmin** - Web UI для БД (порт 5050)

## ⚡ Быстрый старт

```bash
cd /Users/maximviazov/Developer/Golang/zeno-auth

# Запустить весь стек
make local-up

# Подождать 10-15 секунд
make local-status
```

## 🌐 Доступ к сервисам

### 🎨 Frontend (Zeno Console)
- **URL:** http://localhost:5173
- **Что доступно:**
  - Регистрация нового пользователя
  - Логин
  - Просмотр профиля

### 🔧 Backend (Zeno Auth API)
- **Base URL:** http://localhost:8080
- **Health:** http://localhost:8080/health
- **JWKS:** http://localhost:8080/jwks
- **Endpoints:**
  - `POST /auth/register` - Регистрация
  - `POST /auth/login` - Логин
  - `POST /auth/refresh` - Обновление токена
  - `POST /auth/logout` - Выход
  - `GET /me` - Профиль пользователя

### 🗄️ Database (pgAdmin)
- **URL:** http://localhost:5050
- **Login:** `admin@zeno.local`
- **Password:** `admin`

## 🧪 Тестирование

### Через Frontend
1. Открой http://localhost:5173
2. Зарегистрируй нового пользователя
3. Войди в систему
4. Проверь профиль

### Через API (curl)
```bash
# Запустить автоматические тесты
make local-test
```

### Ручное тестирование API

```bash
# Регистрация
curl -X POST http://localhost:8080/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "SecurePass123!",
    "full_name": "Test User"
  }'

# Логин
curl -X POST http://localhost:8080/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "SecurePass123!"
  }'

# Получить профиль (замени TOKEN)
curl http://localhost:8080/me \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

## 🔄 Разработка

### Hot Reload

**Frontend:** Изменения в `/Users/maximviazov/Developer/Web/zeno-console/src` применяются автоматически (Vite HMR)

**Backend:** После изменений нужно пересобрать:
```bash
make local-rebuild
```

### Просмотр логов

```bash
# Все сервисы
make local-logs

# Только backend
docker-compose logs -f zeno-auth

# Только frontend
docker-compose logs -f zeno-console

# Только БД
docker-compose logs -f postgres
```

### Остановка

```bash
# Остановить все
make local-down

# Остановить и удалить данные
make local-clean
```

## 🐛 Отладка

### Frontend не загружается
```bash
# Проверь логи
docker-compose logs zeno-console

# Перезапусти
docker-compose restart zeno-console
```

### Backend не отвечает
```bash
# Проверь health
curl http://localhost:8080/health

# Проверь логи
docker-compose logs zeno-auth
```

### CORS ошибки
CORS уже настроен в backend для работы с любым origin. Если проблемы:
```bash
# Проверь что frontend обращается к http://localhost:8080
# Проверь .env.local в zeno-console
```

### База данных
```bash
# Подключись через psql
docker exec -it zeno-auth-postgres psql -U zeno_auth -d zeno_auth

# Или через pgAdmin: http://localhost:5050
```

## 📊 Архитектура

```
┌─────────────────┐
│   Browser       │
│  localhost:5173 │
└────────┬────────┘
         │ HTTP
         ▼
┌─────────────────┐
│  Zeno Console   │
│   (React/Vite)  │
└────────┬────────┘
         │ API Calls
         ▼
┌─────────────────┐
│   Zeno Auth     │
│   (Go/Gin)      │
└────────┬────────┘
         │ SQL
         ▼
┌─────────────────┐
│   PostgreSQL    │
│   (Database)    │
└─────────────────┘
```

## 🎯 Сценарии тестирования

### 1. Регистрация и логин
1. Открой http://localhost:5173
2. Нажми "Register"
3. Заполни форму
4. После регистрации автоматически залогинишься
5. Увидишь свой профиль

### 2. Работа с токенами
1. Залогинься через frontend
2. Открой DevTools → Application → Local Storage
3. Увидишь `access_token` и `refresh_token`
4. Токены автоматически обновляются при истечении

### 3. API интеграция
1. Получи токен через frontend
2. Используй его для API запросов:
```bash
TOKEN="your_token_here"
curl http://localhost:8080/me -H "Authorization: Bearer $TOKEN"
```

## 🔐 Безопасность

⚠️ **ВАЖНО:** Все ключи и пароли предназначены ТОЛЬКО для локальной разработки!

- JWT ключи - тестовые
- Пароли БД - простые
- CORS - открыт для всех

**НИКОГДА** не используй эти настройки в production!

## 📚 Следующие шаги

После успешной обкатки локально:

1. ✅ Протестируй все flow (регистрация, логин, профиль)
2. ✅ Проверь работу refresh токенов
3. ✅ Убедись что CORS работает корректно
4. ✅ Проверь валидацию форм
5. ✅ Протестируй обработку ошибок
6. 🚀 Готов к деплою в dev окружение!

## 🆘 Помощь

### Полная перезагрузка
```bash
# Останови всё
make local-down

# Удали все данные
make local-clean

# Запусти заново
make local-up
```

### Проверка портов
```bash
# Убедись что порты свободны
lsof -i :5173  # Frontend
lsof -i :8080  # Backend
lsof -i :5432  # PostgreSQL
lsof -i :5050  # pgAdmin
```

### Очистка Docker
```bash
# Если что-то сломалось
docker system prune -a --volumes
make local-up
```

---

**Удачи с разработкой полноценного SaaS! 🎉**
