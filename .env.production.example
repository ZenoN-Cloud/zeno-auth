# ───────────────────────────────────────────────────────
# Production Environment Configuration
# ───────────────────────────────────────────────────────
# ⚠️  NEVER commit this file with real values!
# ⚠️  Use Secret Manager for sensitive data in production
# ───────────────────────────────────────────────────────

# ───────────────
# Base
# ───────────────
ENV=production
APP_NAME=zeno-auth
PORT=8080

# ───────────────
# CORS - STRICT!
# ───────────────
# ⚠️  NEVER use "*" in production!
# Only list your actual frontend domains
CORS_ALLOWED_ORIGINS=https://zeno-cy.com,https://console.zeno-cy.com,https://app.zeno-cy.com

# ───────────────
# Logging
# ───────────────
# In Cloud Run/K8s: ALWAYS use stdout
LOG_LEVEL=info
LOG_FORMAT=json
# LOG_FILE is ignored in production - logs go to stdout

# ───────────────
# Database
# ───────────────
# ⚠️  Load from Secret Manager in production!
# GCP: Secret "zeno-auth-db-dsn"
# AWS: Secrets Manager
DATABASE_URL=postgres://user:pass@host:5432/dbname?sslmode=require

# ───────────────
# JWT Keys
# ───────────────
# ⚠️  CRITICAL: Load from Secret Manager!
# GCP: Secret "zeno-auth-jwt-private-key"
# AWS: Secrets Manager
# NEVER hardcode keys here!
JWT_PRIVATE_KEY="---LOAD-FROM-SECRET-MANAGER---"
JWT_PUBLIC_KEY="---LOAD-FROM-SECRET-MANAGER---"

# ───────────────
# Token TTL
# ───────────────
ACCESS_TOKEN_TTL=900         # 15 minutes (shorter in prod)
REFRESH_TOKEN_TTL=604800     # 7 days (shorter in prod)

# ───────────────
# Misc
# ───────────────
TIMEZONE=UTC
