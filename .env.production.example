# ═══════════════════════════════════════════════════════
# 🚀 PRODUCTION CONFIGURATION
# ═══════════════════════════════════════════════════════
# ⚠️  NEVER commit real secrets to repository!
# ⚠️  Use GCP Secret Manager / AWS Secrets Manager / Vault
# ═══════════════════════════════════════════════════════

# ───────────────
# Base
# ───────────────
ENV=production
APP_NAME=zeno-auth
PORT=8080

# ───────────────
# CORS - STRICT!
# ───────────────
# Only allow your production domains
CORS_ALLOWED_ORIGINS=https://zeno-cy.com,https://console.zeno-cy.com

# ───────────────
# Logging
# ───────────────
LOG_LEVEL=info
LOG_FORMAT=json
# In production, logs go to stdout only (no file)
LOG_FILE=

# ───────────────
# Database
# ───────────────
# Load from Secret Manager in production
DATABASE_URL=postgres://user:password@host:5432/dbname?sslmode=require

# ───────────────
# JWT Keys
# ───────────────
# ⚠️  LOAD FROM SECRET MANAGER - NEVER HARDCODE!
# GCP: gcloud secrets versions access latest --secret="jwt-private-key"
# AWS: aws secretsmanager get-secret-value --secret-id jwt-private-key
JWT_PRIVATE_KEY=
JWT_PUBLIC_KEY=

# ───────────────
# Token TTL
# ───────────────
ACCESS_TOKEN_TTL=1800        # 30 min
REFRESH_TOKEN_TTL=1209600    # 14 days

# ───────────────
# Misc
# ───────────────
TIMEZONE=UTC
